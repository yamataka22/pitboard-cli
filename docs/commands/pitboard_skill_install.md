## pitboard skill install

pitboard スキルを AI エージェントに登録する

```
pitboard skill install [flags]
```

### Options

```
  -h, --help            help for install
      --target string   claude | codex | all (default "claude")
```

### Options inherited from parent commands

```
      --fields string   data に含める列をカンマ区切りで指定（JSON のみ）
      --json            JSON で出力する（パイプ時は自動）
```

### SEE ALSO

* [pitboard skill](pitboard_skill.md)	 - AI エージェントへのスキル登録（install / show）

