# goreleaser v2。タグを push すると GitHub Releases に各 OS 向けバイナリを出す
version: 2

project_name: pitboard

builds:
  - main: ./cmd/pitboard
    binary: pitboard
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md

checksum:
  name_template: checksums.txt

changelog:
  use: github
  sort: asc

# Homebrew tap。公開時に yamataka22/homebrew-tap リポジトリを作ってから有効にする
# brews:
#   - repository:
#       owner: yamataka22
#       name: homebrew-tap
#     homepage: https://pitboard.dev
#     description: pitboard CLI for humans and AI agents
#     license: MIT
#     test: |
#       system "#{bin}/pitboard version"
